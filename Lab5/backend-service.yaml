apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: ClusterIP # set the type of Service
  selector:
    app: backend # referencing the Pod's label
  ports:
  - name: http
    protocol: TCP
    port: 5001
    targetPort: 5001
